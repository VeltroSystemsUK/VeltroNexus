// ADD THIS ROUTE TO App.tsx

// After line 94 (after the /architect route), add:

          <Route path="/ares-architect" element={
            <ProtectedRoute roles={['ADMIN']}>
              <AresArchitectView />
            </ProtectedRoute>
          } />

// The route should be added in the Admin Routes section, right after the /architect route
