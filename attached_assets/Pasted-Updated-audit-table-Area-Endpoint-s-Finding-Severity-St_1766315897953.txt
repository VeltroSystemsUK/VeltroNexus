Updated audit table
#	Area / Endpoint(s)	Finding	Severity	Status on updated code	Evidence	What to change
1	Contacts: GET/POST /api/prospects/:prospectId/contacts, PATCH /api/contacts/:id	IDOR / broken tenancy: any logged-in user can read/create/update contacts for other users by guessing IDs	Critical	❌ Unfixed	server/routes.ts:1457–1464, 1539+, 1551+ and server/storage.ts:450+	Require prospect ownership before list/create; update must enforce ownership via join (contact → prospect → userId)
2	Activities: GET /api/prospects/:prospectId/activities, PATCH/DELETE /api/activities/:id	IDOR: listing/mutations not scoped to user	Critical	❌ Unfixed (creation sets userId, but access still leaks)	server/routes.ts:1704–1711 and server/storage.ts:487+	Filter list by userId or join via prospect ownership; update/delete must include AND userId = ? (or join)
3	Due diligence: GET/PATCH /api/prospects/:prospectId/due-diligence	IDOR: due diligence readable/writable by guessing prospectId	Critical	❌ Unfixed	server/routes.ts:1752–1759 and server/storage.ts:524+	Guard with getProspect(prospectId, userId); storage should join dueDiligence → prospects and filter prospects.userId
4	Lender products: GET/POST /api/lenders/:lenderId/products, PATCH/DELETE /api/lender-products/:id	IDOR: lender products not scoped to owning user	Critical	❌ Unfixed	server/routes.ts:2533–2537 and server/storage.ts:623+	Join lenderProducts → lenders, filter lenders.userId; update/delete must enforce ownership via join
5	Lender interactions: GET /api/lenders/:lenderId/interactions, PATCH/DELETE /api/lender-interactions/:id	Partial tenancy: you added user-scoped listing, but lenderId-scoped list + update/delete can still leak/modify across users	High	⚠️ Partially improved	Leaky list: server/routes.ts:2584–2588, server/storage.ts:693+; new safe list: server/routes.ts:2595–2600, server/storage.ts:701+; update: server/storage.ts:731+	Either remove lenderId-scoped list or require userId filter; update/delete must enforce interaction.userId === req.user
6	Underwriting: GET /api/underwriting/download/:submissionId/:activityId/:fileIndex	Over-broad access: any user with role === 'underwriter' can download any submission	High	❌ Unfixed	server/routes.ts:4385–4401	Require assigned underwriter (submission.underwriterId === userId) and/or admin role, not “any underwriter”
7	Sessions	TTL unit risk: connect-pg-simple ttl is being set using milliseconds (often expected seconds) → sessions could last wildly longer than intended	High	❌ Unfixed	server/replitAuth.ts:22–28	Change to ttl: Math.floor(sessionTtl / 1000); keep cookie.maxAge in ms
8	Error handling (many routes)	Information leakage: lots of res.status(500).json({ error: error.message }) despite having safe error utilities	Medium	⚠️ Partially improved	Example: server/routes.ts:1462–1464 (and many others)	Standardise error responses: use createErrorResponse(...) everywhere or throw to global handler
9	Downloads	Header injection / unsafe filenames: Content-Disposition uses user-controlled filenames	Medium	❌ Unfixed	server/routes.ts:4422, 4744	Sanitize filenames (strip CR/LF, quotes, semicolons) or use RFC5987 encoding
10	Logo upload + public serving	SVG allowed (image/* check + served as image/svg+xml) — SVG can become an XSS footgun depending on how it’s used later	Medium	❌ Unfixed	MIME allow: server/routes.ts:213; serving: server/routes.ts:340	Disallow SVG, or sanitise SVG server-side; ensure rendered only via <img> and consider stricter CSP
11	Packaging / repo hygiene	Zip still contains .git/ + node_global cache paths	Low	❌ Unfixed	Zip contents include FlowLoan/.git/ and FlowLoan/.config/npm/node_global/...	Exclude from deployment artifacts (.dockerignore/build filters); keeps deploys smaller + reduces info exposure