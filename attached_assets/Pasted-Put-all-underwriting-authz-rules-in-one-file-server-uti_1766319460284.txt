Put all underwriting authz rules in one file (server/utils/underwritingAuth.ts)

Use two tiny middlewares:

requireSubmissionReadAccess({ allowTriage: true })

requireSubmissionWriteAccess()

Those middlewares:

load the submission once

load the user once

enforce the rule

attach { submission, user } onto req.ctx so every route stays tidy

Then each endpoint becomes 3–6 lines of readable code.

1) Add a tiny auth module
Create: server/utils/underwritingAuth.ts
import type { Request, Response, NextFunction } from "express";

type Role = "broker" | "underwriter" | "sales_admin" | "super_admin" | string;

export type AuthUser = {
  id: string;
  role?: Role;
};

export type UnderwritingSubmission = {
  id: number;
  brokerId: string;
  status?: string | null;
  assignedUnderwriterId?: string | null;
};

export type Ctx = {
  user: AuthUser;
  submission: UnderwritingSubmission;
};

declare global {
  namespace Express {
    interface Request {
      ctx?: Ctx;
      user?: any; // (you already have req.user via your auth)
    }
  }
}

function isSuperAdmin(role?: Role) {
  return role === "super_admin";
}

function isUnderwriter(role?: Role) {
  return role === "underwriter";
}

function canReadSubmission(
  submission: UnderwritingSubmission,
  user: AuthUser,
  opts: { allowTriage?: boolean }
) {
  if (isSuperAdmin(user.role)) return true;

  const isSubmittingBroker = submission.brokerId === user.id;
  const isAssignedUnderwriter = submission.assignedUnderwriterId === user.id;

  if (isSubmittingBroker || isAssignedUnderwriter) return true;

  // Optional triage view: underwriters can view unassigned + submitted
  const triageAllowed =
    !!opts.allowTriage &&
    isUnderwriter(user.role) &&
    submission.status === "submitted" &&
    (submission.assignedUnderwriterId == null);

  return triageAllowed;
}

function canWriteSubmission(submission: UnderwritingSubmission, user: AuthUser) {
  if (isSuperAdmin(user.role)) return true;

  // Brokers don't "write" underwriting workflow (except withdraw, handled elsewhere)
  if (submission.brokerId === user.id) return false;

  // Only assigned underwriter can mutate
  return isUnderwriter(user.role) && submission.assignedUnderwriterId === user.id;
}

export function requireSubmissionReadAccess(params: {
  storage: any;
  allowTriage?: boolean;
}) {
  const { storage, allowTriage } = params;

  return async (req: Request, res: Response, next: NextFunction) => {
    try {
      const userId = req.user?.claims?.sub;
      if (!userId) return res.status(401).json({ error: "Unauthenticated" });

      const submissionId = Number(req.params.id);
      if (!Number.isFinite(submissionId)) {
        return res.status(400).json({ error: "Invalid submission id" });
      }

      const [user, submission] = await Promise.all([
        storage.getUser(userId),
        storage.getUnderwritingSubmission(submissionId),
      ]);

      if (!submission) return res.status(404).json({ error: "Not found" });

      const authUser: AuthUser = { id: userId, role: user?.role };
      if (!canReadSubmission(submission, authUser, { allowTriage })) {
        return res.status(403).json({ error: "Access denied" });
      }

      req.ctx = { user: authUser, submission };
      next();
    } catch (err) {
      next(err);
    }
  };
}

export function requireSubmissionWriteAccess(params: { storage: any }) {
  const { storage } = params;

  return async (req: Request, res: Response, next: NextFunction) => {
    try {
      const userId = req.user?.claims?.sub;
      if (!userId) return res.status(401).json({ error: "Unauthenticated" });

      const submissionId = Number(req.params.id);
      if (!Number.isFinite(submissionId)) {
        return res.status(400).json({ error: "Invalid submission id" });
      }

      const [user, submission] = await Promise.all([
        storage.getUser(userId),
        storage.getUnderwritingSubmission(submissionId),
      ]);

      if (!submission) return res.status(404).json({ error: "Not found" });

      const authUser: AuthUser = { id: userId, role: user?.role };
      if (!canWriteSubmission(submission, authUser)) {
        return res.status(403).json({ error: "Only the assigned underwriter can do that" });
      }

      req.ctx = { user: authUser, submission };
      next();
    } catch (err) {
      next(err);
    }
  };
}

2) Use the helper in routes (so routes stay “ultra clean”)

In server/routes.ts, import:

import {
  requireSubmissionReadAccess,
  requireSubmissionWriteAccess,
} from "./utils/underwritingAuth";


Now your endpoints become tidy.

A) GET single submission (triage read allowed)
app.get(
  "/api/underwriting/submissions/:id",
  isAuthenticated,
  requireSubmissionReadAccess({ storage, allowTriage: true }),
  async (req: any, res) => {
    const { submission } = req.ctx;
    const enriched = await enrichSubmissions([submission]);
    res.json(enriched[0]);
  }
);

B) PATCH submission (write access only)
app.patch(
  "/api/underwriting/submissions/:id",
  isAuthenticated,
  requireSubmissionWriteAccess({ storage }),
  async (req: any, res, next) => {
    try {
      const { submission, user } = req.ctx;

      // If you have a broker-withdraw path, keep that separate in its own route
      // or explicitly check here based on user.role.

      const updated = await storage.updateUnderwritingSubmission(submission.id, req.body);
      res.json(updated);
    } catch (e) {
      next(e);
    }
  }
);

C) POST comment (no triage — must be owner/assigned/admin)

You have two ways to do this:

Option 1 (recommended): use ReadAccess without triage
app.post(
  "/api/underwriting/submissions/:id/comments",
  isAuthenticated,
  requireSubmissionReadAccess({ storage, allowTriage: false }),
  async (req: any, res, next) => {
    try {
      const { submission, user } = req.ctx;
      const comment = await storage.addUnderwritingComment(submission.id, user.id, req.body.content);
      res.json(comment);
    } catch (e) {
      next(e);
    }
  }
);

D) GET activities (no triage)
app.get(
  "/api/underwriting/submissions/:id/activities",
  isAuthenticated,
  requireSubmissionReadAccess({ storage, allowTriage: false }),
  async (req: any, res, next) => {
    try {
      const { submission } = req.ctx;
      const activities = await storage.listUnderwritingActivities(submission.id);
      res.json(activities);
    } catch (e) {
      next(e);
    }
  }
);

3) Clean up the list endpoint (queue + my assigned)

This one is better handled via storage filters (as we discussed), but you can still keep it clean:

app.get("/api/underwriting/submissions", isAuthenticated, async (req: any, res, next) => {
  try {
    const userId = req.user.claims.sub;
    const user = await storage.getUser(userId);

    const isSuperAdmin = user?.role === "super_admin";
    const { status, assigned } = req.query;

    let submissions;
    if (isSuperAdmin) {
      submissions = await storage.listUnderwritingSubmissions({
        status: status ? String(status) : undefined,
        assignedUnderwriterId: assigned === "me" ? userId : undefined,
      });
    } else {
      submissions = await storage.listUnderwritingSubmissionsForUnderwriter(userId, {
        status: status ? String(status) : undefined,
        assigned: assigned === "me" ? "me" : assigned === "queue" ? "queue" : "all",
      });
    }

    const enriched = await enrichSubmissions(submissions);
    res.json(enriched);
  } catch (e) {
    next(e);
  }
});

4) “Claim” atomic fix (still worth doing)

In storage.ts update claim to only succeed if unassigned+submitted. This prevents races:

.where(and(
  eq(underwritingSubmissions.id, id),
  eq(underwritingSubmissions.status, "submitted"),
  sql`${underwritingSubmissions.assignedUnderwriterId} is null`
))


Then in the route:

if (!updated) return res.status(409).json({ error: "Submission already claimed" });

5) /api/lenders/:id/full tidy fix

Replace:

storage.listLenderInteractions(lenderId)


With:

storage.listLenderInteractions(lenderId, userId)

Step-by-step instructions to apply safely

Add the new file server/utils/underwritingAuth.ts

Import and wrap these endpoints first (lowest risk):

GET /api/underwriting/submissions/:id (allowTriage: true)

GET /api/underwriting/submissions/:id/activities (allowTriage: false)

POST /api/underwriting/submissions/:id/comments (allowTriage: false)

Wrap PATCH with requireSubmissionWriteAccess

Fix claim to be atomic + return 409

Fix /lenders/:id/full userId parameter

Quick test:

Underwriter U1 claims → U2 should get 403 on view/comment/activities (except queue list)

U2 can still see queue items but not U1-assigned items

Broker only sees their own